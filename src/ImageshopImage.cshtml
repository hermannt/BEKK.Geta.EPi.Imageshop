@model Geta.EPi.Imageshop.ImageshopImage

@if (Model != null && !string.IsNullOrWhiteSpace(Model.Url))
{
    <img src="@Model.Url" height="@Model.Height" width="@Model.Width" alt="@ViewData["ImageAltText"]">
}